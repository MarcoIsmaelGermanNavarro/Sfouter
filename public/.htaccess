<IfModule mod_rewrite.c>
    RewriteEngine On

    # 1. Redirigir todo lo que no sea un archivo o carpeta real a index.php
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php [QSA,L]
</IfModule>